
%h4
  赎回投资列表
= will_paginate @invests
%table.table.table-bordered.table-striped.table-hover
  %thead
    %tr
      %th 编号
      -#%th 已预约金额
      %th 加入日
      %th 转让日
      %th 投资金额
      %th 利期数
      %th 本期数
      %th 产品编号
      %th 投资帐号
      %th 操作

  %tbody
    - @invests.each do |inv|
      %tr
        %td
          = inv.invest_number
        %td
          = inv.created_at.strftime("%Y.%m.%d %H:%M")
        %td
          = inv.updated_at.strftime("%Y.%m.%d %H:%M")
        %td
          = inv.amount
        %td
          = 2
        %td
          = 2
        %td
          = link_to inv.product.deposit_number, show_products_path(inv.product.product_type, inv.product.id)
        %td
          = link_to inv.user_info.user.username, user_path(:id => inv.user_info.user.id)
        %td
          = link_to '通过', resale_confirm_invests_path(:invest_id => inv.id)
          = link_to '回收', user_path(:id => inv.user_info.user.id)


